/** @type {import('next').NextConfig} */
const nextConfig = {
  // The lint setup is optional for this project; don't fail the build on it.
  eslint: { ignoreDuringBuilds: true },
};

export default nextConfig;
